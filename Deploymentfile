sudo apt install -y nginx
sudo nano /etc/nginx/nginx.conf
sudo nano /etc/nginx/conf.d/app.conf

server {
    listen 80;
    server_name l.shivam-singh.in;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d l.shivam-singh.in

npm init -y
npm install express mongoose bcrypt express-session dotenv

npm i

docker build -t linkmgr1 .
docker run --rm -d -p 3000:3000 --name linkmgr1 --env-file .env linkmgr1

.env
PORT=3000
MONGODB_URI=mongodb+srv://Mongo:*****mongodbpass****@production.nskmfpu.mongodb.net/linkmgr?retryWrites=true&w=majority
MONGODB_DB=linkmgr
SESSION_SECRET=secratepassword
